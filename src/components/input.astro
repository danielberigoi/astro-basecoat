---
import type { HTMLAttributes } from "astro/types";

interface Props extends HTMLAttributes<"input"> {
	invalid?: boolean;
}

const { invalid, class: className, ...rest } = Astro.props;
---

<input
	class:list={["input", className]}
	aria-invalid={invalid ? "true" : undefined}
	{...rest}
/>
